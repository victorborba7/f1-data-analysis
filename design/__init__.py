"""F1 Analytics design system.

Single source of truth: ``design/tokens.yml``. Regenerate the downstream
artifacts under ``design/generated/`` with::

    python design/build_tokens.py

Python charts import the generated palette via ``design.generated.tokens``.
"""
