packages:
  # Surrogate keys, generic tests (unique_combination_of_columns, etc.), helpers.
  - package: dbt-labs/dbt_utils
    version: [">=1.1.0", "<2.0.0"]
  # Richer data-quality tests: value ranges, row-count expectations, regex.
  - package: calogica/dbt_expectations
    version: [">=0.10.0", "<0.11.0"]
